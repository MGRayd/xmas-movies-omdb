rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default rule - deny access
    match /{allPaths=**} {
      // Simple rule - anyone can read, authenticated users can write
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
